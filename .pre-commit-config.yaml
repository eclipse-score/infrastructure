repos:
  - repo: local
    hooks:
      - id: aggregate-status
        name: Aggregate chapter statuses
        entry: python scripts/aggregate_status.py
        language: python
        pass_filenames: false
        files: ^(docs/.*\.md|scripts/aggregate_status\.py)$
        exclude: ^docs/index\.md$
